# ADaCITra v0.4.2 common Python dependencies
# Tested with Python 3.10.
#
# Install the platform-specific PyTorch build before installing this file:
# - Desktop PC: use the appropriate CPU or CUDA build from pytorch.org.
# - NVIDIA Jetson: use the NVIDIA JetPack-compatible PyTorch build.

ultralytics>=8.3.230,<8.5.0
opencv-python>=4.5.4,<5.0.0
numpy>=1.23,<3.0
Pillow>=9.0,<12.0
PyYAML>=6.0,<7.0

# tkinter is part of the standard Python installation on Windows.
# On Ubuntu/Jetson, install it through the operating system package manager:
# sudo apt install python3-tk
